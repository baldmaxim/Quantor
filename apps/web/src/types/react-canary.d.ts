/**
 * Типы канареечного React.
 *
 * Next.js подменяет `react` на свою канареечную сборку при компиляции — оттуда
 * берётся `<ViewTransition>`. В `@types/react` эти объявления лежат отдельным
 * файлом `react/canary` и подключаются вот таким пустым импортом.
 */
import {} from 'react/canary';
