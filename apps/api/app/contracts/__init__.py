"""Контракты будущих сервисов.

Здесь нет ни одной реализации и ни одного вызова наружу. Модули этого пакета существуют,
чтобы Stage 2 подключался к готовым границам, а не переделывал оболочку.

Проверяемое свойство: в зависимостях портала нет ни одного SDK моделей и ни одной
библиотеки компьютерного зрения — см. tests/test_contracts.py.
"""
