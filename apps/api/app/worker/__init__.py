"""Исполнитель заданий.

Отдельный процесс, а не отдельное приложение: воркеру нужны те же модели, сервисы и
настройки, что и API, и третий пакет означал бы либо дублирование, либо ещё один общий
пакет. Запускается как `python -m app.worker` (ADR-0015).
"""
