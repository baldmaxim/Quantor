"""Аутентификация и авторизация.

Разделение модулей соответствует вопросам, на которые они отвечают:

- `permissions` — что вообще бывает разрешено и какая роль что включает;
- `context` — кто спрашивает и что ему можно в этом запросе;
- `resolver` — как этот контекст построить и как потребовать право;
- `sessions` — сеанс браузера строкой в базе;
- `csrf` — подтверждение небезопасного запроса;
- `oidc` — разговор с провайдером входа;
- `dev` — фиксированная личность для локальной работы, без обращений к базе.

Пароли не хранятся ни в одном из них: личность удостоверяет внешний провайдер (ADR-0012).
"""
