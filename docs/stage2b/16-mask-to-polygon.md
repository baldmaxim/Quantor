# Промт 16. Маска → многоугольник с отверстиями

Дата: 2026-09-15. Выполнен до промтов 13–15 по решению владельца ([Р-7](owner-decisions.md)): этап не
зависит ни от GPU, ни от результатов Qwen. Пакет называет документ `13-mask-to-polygon.md`; номер
документа здесь = номер промта, как у остальных отчётов Stage 2B.

## Статус

| Часть                                         | Статус                                                                                     |
| --------------------------------------------- | ------------------------------------------------------------------------------------------ |
| векторизатор, сшивка тайлов, метрики, команда | код готов: `vision/quantor_vision/vector/`, `vision vectorize slab`                        |
| синтетические тесты (список промта)           | написаны, `vision/tests/test_vector.py`; ruff чист; **pytest и mypy — у владельца и в CI** |
| прогон на прогнозах U-Net и SAM, val          | выполнен владельцем 2026-09-15, настройка `68f8e0318eaf` (§ 8)                             |
| test — один раз той же настройкой             | выполнен владельцем 2026-09-15 для обоих прогонов (§ 9)                                    |

## 1. Что взято из SU10

Разбор передачи SU10 (2026-09-14) показал, что у коллеги этот шаг уже есть в чистом виде:
`area_candidate_geometry_v001.py` — обход пиксельных рёбер без потерь, отверстия по вложенности.
Взяты алгоритм обхода (поворот к своей клетке на диагональном касании) и привязка отверстия к
наименьшему охватывающему кольцу; код переписан без numpy и типизирован. Добавлено то, чего у SU10 нет
и без чего кандидат не пройдёт портал:

- **разведение перетяжек:** вершина, через которую граница проходит дважды, сдвигается на
  `pinch_nudge_px` внутрь своей клетки — касание в промте 05 — пересечение, а не валидный контур;
- **упрощение с ограничением площади:** Дуглас — Пекер с откатом допуска вдвое, пока изменение
  нетто-площади ≤ `max_area_change_ratio` и контур валиден; последний вариант — без упрощения;
- **проверка валидности портала:** `vector/validity.py` — копия `apps/api/.../geometry/validity.py`;
  тест сверяет текст построчно (кроме импорта) и значения кодов с `GeometryIssueCode`;
- **сшивка тайлов голосованием** и **метрики против векторной разметки**.

## 2. Конвейер

```text
маски тайлов <run>/predictions/<split>/*.png    (SHA-256 сверяется с <split>-predictions.jsonl)
→ сшивка листа: доля покрывающих тайлов с плитой ≥ vote_threshold (0,5; ничья — плита)
→ морфология: нет (польза на val не проверена)
→ рёбра пикселей → кольца → углы               площадь кольца = число пикселей, точно
→ отверстие → наименьшее охватывающее внешнее кольцо
→ отверстия < min_hole_px заливаются, области < min_component_px отбрасываются — со счётчиками
→ разведение перетяжек → упрощение с откатом → нормализованные координаты листа
→ валидность промта 05: недействительный — status=invalid с кодом, площади не даёт
```

Связность: плита 4-связна, фон 8-связен. Пустой пиксель, касающийся наружного фона углом, — не
отверстие, а перетяжка внешнего кольца.

Координаты: рабочий пиксель (x, y) → `x · downsample / source_width` (как `TileTransform` промта 08),
отсчёт от левого верхнего угла листа. Масштаба и метров здесь нет.

## 3. Настройка — `vectorize_polygon_v1`

| Ключ                    | По умолчанию | Смысл                                                         |
| ----------------------- | -----------: | ------------------------------------------------------------- |
| `vote_threshold`        |          0,5 | доля тайлов с плитой для пикселя в перекрытии                 |
| `min_component_px`      |          256 | меньше — обрывок (16×16 рабочих px = 32×32 px TIFF)           |
| `min_hole_px`           |           64 | меньше — шум, заливается                                      |
| `simplify_tolerance_px` |          1,5 | стартовый допуск упрощения                                    |
| `min_tolerance_px`      |         0,25 | ниже — без упрощения                                          |
| `max_area_change_ratio` |         0,01 | предел изменения нетто-площади многоугольника упрощением      |
| `pinch_nudge_px`        |         0,05 | сдвиг вершины перетяжки                                       |
| `edge_budget`           |    4 000 000 | больше рёбер на листе — `too_complex`, не зависание           |
| `metric_stride_px`      |            2 | шаг растеризации метрик покрытия                              |
| `boundary_tolerance_px` |            3 | допуск Boundary F1 (6 px TIFF)                                |
| `coverage_to_recall`    |          0,5 | доля покрытия, чтобы плита или отверстие считались найденными |

Значения объявлены до val. Меняются только `--set` на val; хеш настройки — имя каталога результатов.
Test принимает только настройку, у которой уже есть val-метрики, и только один раз.

## 4. Метрики против разметки PlanSwift

| Метрика                        | Как считается                                                               |
| ------------------------------ | --------------------------------------------------------------------------- |
| ошибка нетто-площади листа     | Σ(внешнее − отверстия) годных многоугольников против той же суммы разметки  |
| ложная площадь листа без плиты | площадь прогноза / площадь листа — закрывает пробел метрики промта 11       |
| полнота плит                   | плита разметки покрыта прогнозом ≥ 0,5 её площади                           |
| точность многоугольников       | многоугольник прогноза лежит в плитах разметки ≥ 0,5 своей площади          |
| полнота отверстий              | отверстие найденной плиты оставлено пустым ≥ 0,5 его площади                |
| граница                        | точки через 1 px вдоль колец; F1 при допуске 3 px; медиана и p90 расстояния |
| недействительные               | доля и коды промта 05                                                       |
| вершины                        | углы решётки → вершины после упрощения, коэффициент сжатия                  |

Разметка читается из `planswift-gt-v1` (`annotations.jsonl`) с отбором и снятием дублей, как у
сборщика промта 08; перекрывающиеся плиты разметки в сумме площади считаются дважды — так же, как
их посчитал бы `area_with_holes.v1` по отдельным измерениям.

## 5. Артефакты и происхождение

```text
<run>/vector/<config12>/config.json            настройка и SHA-256
<run>/vector/<config12>/<split>/<page>.json    многоугольники, коды, хеши масок-источников, digest
<run>/vector/<config12>/<split>-metrics.json   сводка, листы, хеши манифеста прогнозов, tiles и разметки
```

Маска остаётся доказательством; вектор — производный артефакт с версией, хешем настройки и хешами
масок. Изменённая после прогноза маска — отказ.

## 6. Шаги для владельца

После `git pull`, окружение `.venv-train`. `$B` — сборка, `$C` — конфиг сборки с путями разметки.

```powershell
cd vision
$B = "$env:QUANTOR_DATASET_ROOT\planswift\build\planswift-build-v1"
$C = "$env:QUANTOR_DATASET_ROOT\planswift\build-v1.json"
$R = "$env:QUANTOR_DATASET_ROOT\runs"
.\.venv-train\Scripts\python -m quantor_vision vectorize slab --build $B --run "$R\slab-tinyunet-b16-v1" --build-config $C --split val
.\.venv-train\Scripts\python -m quantor_vision vectorize slab --build $B --run "$R\sam-coarse-small-v1" --build-config $C --split val
```

Прислать `summary` обоих. Test — только после ответа по val и без `--set`:

```powershell
.\.venv-train\Scripts\python -m quantor_vision vectorize slab --build $B --run "$R\slab-tinyunet-b16-v1" --build-config $C --split test
.\.venv-train\Scripts\python -m quantor_vision vectorize slab --build $B --run "$R\sam-coarse-small-v1" --build-config $C --split test
```

Проверка кода: `pnpm test:vision` из корня — прислать вывод при ошибках.

## 7. Ограничения

- Контур — пиксельная ступенька рабочего растра, упрощённая с ограничением площади: это не
  «родная» линия чертежа. Точность границы ограничена разрешением прогноза (1 рабочий px = 2 px TIFF).
- Полнота плит и отверстий — по покрытию площади, а не по сопоставлению объектов; сопоставление
  (минимальная стоимость, как `prepared_min_cost_matching.py` SU10) нужно, когда появятся экземпляры.
- Ошибка площади листа суммирует разметку без объединения перекрытий.
- Скорость — чистый Python на обходе границы: лист с шумным прогнозом упирается в `edge_budget`.

## 8. Результаты val (2026-09-15)

Настройка по умолчанию `68f8e0318eaf`, объявленная до val, без подбора. Val: 150 тайлов, 5 листов с
плитой, 10 плит разметки.

| Метрика                                   | U-Net `slab-tinyunet-b16-v1` | SAM `sam-coarse-small-v1` |
| ----------------------------------------- | ---------------------------: | ------------------------: |
| ошибка нетто-площади листа, медиана       |                        0,183 |                 **0,110** |
| ошибка площади листа, максимум (5 листов) |                     **4,01** |                      4,39 |
| ложная площадь листа без плиты, максимум  |                        0,328 |                 **0,305** |
| полнота плит                              |                         0,30 |                  **0,40** |
| точность многоугольников                  |                    **0,128** |                     0,114 |
| полнота отверстий                         |                    **0,275** |                     0,146 |
| Boundary F1 (3 px)                        |                        0,018 |                 **0,055** |
| недействительные                          |                            0 |                         0 |
| многоугольников (плит в разметке — 10)    |                          539 |                       202 |
| сжатие вершин                             |                         5,44 |                      3,67 |

### Разбор

- **Векторизация не добавляет ошибки.** Медиана ошибки площади листа по вектору совпадает с растровой
  оценкой промтов 10–11 (U-Net 0,183 против 0,205; SAM 0,110 против 0,108), недействительных
  многоугольников ноль, упрощение сжимает вершины в 3,7–5,4 раза при изменении площади ≤ 1 %.
  Конвейер промта 16 годится как общий путь для любой модели.
- **Узкое место — модели.** На 10 плит разметки — 202–539 многоугольников, точность 0,11–0,13, полнота
  0,30–0,40. На листе без плиты прогноз закрашивает до трети листа — эту ошибку прежняя растровая
  оценка не видела.
- **Фильтр малых областей не подбирается.** Подбор `min_component_px` на 5 листах val поднял бы
  точность, но не полноту и не границы и был бы подгонкой под val; проверяется модель, а не пост-обработка.
- **SAM против U-Net:** SAM лучше по площади, полноте плит и границе, U-Net — по отверстиям: SAM
  разворачивает подсказку в замкнутый контур и заливает мелкие отверстия.

## 9. Результаты test (2026-09-15) — один раз

Настройка `68f8e0318eaf`, без `--set`. Test: 56 тайлов, 5 листов — 1 с плитой (20 плит, 205 отверстий
разметки), 4 без плиты.

| Метрика                                  | U-Net `slab-tinyunet-b16-v1` | SAM `sam-coarse-small-v1` |
| ---------------------------------------- | ---------------------------: | ------------------------: |
| ошибка нетто-площади листа с плитой      |                        0,869 |                 **0,652** |
| ложная площадь листа без плиты, максимум |                    **0,529** |                     0,556 |
| полнота плит (покрытие ≥ 0,5)            |                          1,0 |                       1,0 |
| точность многоугольников                 |                        0,017 |                 **0,030** |
| полнота отверстий                        |                            0 |                         0 |
| Boundary F1 (3 px)                       |                        0,000 |                 **0,006** |
| медиана расстояния границы, px           |                ≥ 12 (предел) |             ≥ 12 (предел) |
| недействительные                         |                            0 |                         0 |
| многоугольников                          |                          178 |                        99 |
| сжатие вершин                            |                         6,75 |                      3,37 |

### Разбор

- **Test подтверждает val и хуже его.** Ошибка площади единственного листа с плитой — 87 % и 65 %
  (растровая оценка промтов 10–11 — 85,5 % и 65,5 %: вектор снова совпадает с маской). Ни одно из 205
  отверстий не найдено: модели заливают термовкладыши и капители целиком.
- **Листы без плиты — худшая ошибка.** Прогноз закрашивает до 53–56 % листа. Модель, дающая такое,
  непригодна для кандидатов даже с проверкой человеком.
- **«Полнота плит 1,0» — не успех, а слабость метрики.** Покрытие ≥ 0,5 площади плиты засчитывается и
  при заливке всего листа; при точности 0,02–0,03 и Boundary F1 ≈ 0 это следствие переразметки, а не
  находки. Для промта 18 полнота плит считается по сопоставлению объектов с IoU (минимальная
  стоимость, как `prepared_min_cost_matching.py` SU10), а не по покрытию; числа этого test не
  пересчитываются задним числом.
- **Граница не держится:** медиана расстояния упирается в предел поиска 12 px.
- **Конвейер векторизации исправен** на обеих частях: 0 недействительных, площадь сохраняется.

**Итог промта 16.** Маска → многоугольник с отверстиями работает и не вносит ошибки; гейт slab не
пройден ни U-Net, ни SAM — это нижние границы для промта 18.
