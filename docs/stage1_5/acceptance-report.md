# Приёмка Stage 1.5

Дата: 2026-09-08.

Отчёт разделён на две части, и это разделение — главное в нём. Всё, что помечено
«измерено», получено запуском; всё остальное названо неизмеренным прямо, без округления
в свою пользу. Отчёт, в котором не отличить одно от другого, хуже отсутствующего:
по нему принимают решение о выставлении портала наружу.

## Матрица безопасности

Двенадцать пунктов промта 11. Автоматические проверки собраны в
[test_security_matrix.py](../../apps/api/tests/test_security_matrix.py) — в одном файле
намеренно: приёмка отвечает на один вопрос целиком, а не по крупицам из двадцати файлов.

| #   | Требование                                       | Как проверено                                                        | Итог     |
| --- | ------------------------------------------------ | -------------------------------------------------------------------- | -------- |
| 1   | Неаутентифицированный не читает проекты          | `GET /api/v1/projects` без учётных данных → 401 + `WWW-Authenticate` | **PASS** |
| 2   | Читатель не меняет                               | `POST /projects` под ролью `viewer` → 403                            | **PASS** |
| 3   | Инженер не администратор                         | состав `ROLE_PERMISSIONS[engineer]`                                  | **PASS** |
| 4   | Администратор пространства не выходит за границу | чужой проект → 404                                                   | **PASS** |
| 5   | Доступ администратора платформы явный            | без заголовка → 404, с заголовком → 200                              | **PASS** |
| 6   | Угаданный UUID не пробивает границу              | три маршрута чужого проекта → 404 с кодом `NOT_FOUND`                | **PASS** |
| 7   | Ссылка на файл подчиняется границе               | `content-url` чужой ревизии → 404                                    | **PASS** |
| 8   | Административный API запрещает по умолчанию      | перебор всех операций контракта: у каждой объявлено право            | **PASS** |
| 9   | Секрет не попадает в контракт, ответ и бандл     | поиск по именам полей в OpenAPI и в сгенерированном клиенте          | **PASS** |
| 10  | Разрушительное действие в журнале                | изменение настройки оставляет запись с актором                       | **PASS** |
| 11  | Service worker не кэширует привилегированное     | `/api/` и чужой origin пропускаются; у админки SW нет вовсе          | **PASS** |
| 12  | Выход обесценивает сеанс                         | после `revoke` токен не разрешается                                  | **PASS** |

Пункт 9 проверяет **имена полей**, а не значения: значение может быть пустым на одной
машине и заполненным на другой, а поле с таким именем опасно само по себе.

Пункт 11 проверяет исходник service worker, а не поведение браузера. Правило простое —
`/api/` не кэшируется никогда, — и прогон в настоящем браузере проверял бы браузер,
а не наше решение.

## Измерено запуском

### Контракт и объём

| Величина                | Значение                            |
| ----------------------- | ----------------------------------- |
| Операций в OpenAPI      | 46 (было 21 на входе в этап)        |
| из них административных | 21                                  |
| Таблиц в базе           | 15 (было 7)                         |
| Миграций                | 6, головная `0006_job_worker_lease` |
| Настроек в реестре      | 3                                   |
| Флагов возможностей     | 12 — все ключи Stage 1 сохранены    |
| Разрешений              | 21                                  |
| Ролей                   | 6                                   |
| Действий журнала        | 14                                  |

### Тесты

| Набор               | Значение                        |
| ------------------- | ------------------------------- |
| pytest, собрано     | 425                             |
| vitest, портал      | 139                             |
| vitest, админка     | 8                               |
| Playwright, админка | 17                              |
| Playwright, портал  | 23 сценария на пяти разрешениях |

Числа прохождения на стенде с базой — в разделе «Требует стенда»: на машине разработки
Docker не поднимается, и 136 тестов базы данных здесь пропускаются.

### Изоляция бандла

| Величина                            | Значение    |
| ----------------------------------- | ----------- |
| Чанки JS админки                    | 1,3 МБ      |
| Чанки JS портала                    | 1,7 МБ      |
| `PDFDocumentProxy` в бандле админки | не найден   |
| `pdfjs-dist` в зависимостях админки | отсутствует |

Изоляция держится на отсутствии зависимости, а не на разделении кода: разделение можно
нечаянно сломать одним импортом, отсутствие зависимости — нельзя. Проверяется дважды:
модульным тестом по `package.json` и сценарием Playwright, который скачивает все скрипты
страницы и ищет в них код просмотрщика.

### Поведение под отказами

| Проверка                                           | Итог                                                                     |
| -------------------------------------------------- | ------------------------------------------------------------------------ |
| Мёртвый исполнитель не делает API неготовым        | `/health/ready` → 200 `degraded`, компонент `required: false`            |
| Одна мёртвая проба не вешает диагностику           | остальные компоненты отвечают, мёртвый — `unavailable`                   |
| Общий бюджет ограничивает страницу диагностики     | шесть зависших проб при бюджете 0,5 с → ответ быстрее 3 с, все `unknown` |
| Отказ пробы не выносит строку подключения          | пароль из DSN отсутствует в ответе                                       |
| Отказ TenderHUB не мешает смотреть импортированное | список, карточка и документы отвечают 200                                |
| Задание не достаётся двум исполнителям             | два соединения, `SKIP LOCKED` → ровно один победитель                    |
| Отсутствие опроса на панелях                       | шесть секунд после загрузки — ноль запросов; по кнопке ровно один        |

### Регресс Stage 1

Ни одна проверка Stage 1 не ослаблена. Изменены три и только осознанно, каждая
в одном коммите с изменением, которое её потребовало:

- `test_health.py` — набор компонентов стал надмножеством: добавился `job_worker`;
- `test_jobs_service.py` — у отказа появился ровно один выход, в очередь, и только
  через повтор. Успех и отмена остались окончательными;
- `test_meta.py` — этап стал `stage-1.5`.

## Требует стенда

Здесь Docker не запускается, поэтому ниже — то, что подтверждается только на живой
установке. Прочерк означает «не измерено», а не «ноль».

| Проверка                                                     | Состояние                                                    |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| pytest на живой базе                                         | подтверждено разработчиком: **409 из 409**, ноль пропущенных |
| `alembic upgrade head` / `check` / `downgrade base`          | подтверждено разработчиком: OK                               |
| Первая загрузка `/projects`                                  | —                                                            |
| Первая загрузка панели админки                               | —                                                            |
| Пагинация журнала на 1000 записях                            | —                                                            |
| Импорт эталонного пакета через отдельный воркер              | —                                                            |
| Открытие того же PDF с областями после входа                 | —                                                            |
| Отказ в `content-url` из чужого пространства на живых данных | —                                                            |
| Установка PWA на настоящем iPhone и Android                  | —                                                            |
| `position: fixed` с открытой клавиатурой в Safari            | —                                                            |

Список ручных проверок продублирован в самом наборе тестов, чтобы не потеряться:
`test_manual_checks_are_declared`.

## Известные ограничения этапа

- **Presigned-ссылка остаётся предъявительской.** Выдаётся только после проверки прав,
  но выданная живёт свой срок независимо от сеанса. Сокращение срока жизни до нужд
  просмотрщика — открытый вопрос.
- **Управления пространствами и пользователями из админки нет.** Разрешения заведены,
  разделы объявлены, API управления доступом появится отдельно; сейчас членство
  заводится в базе.
- **Поставщики моделей только читаются.** Описываются развёртыванием; управление
  появится вместе со шлюзом на Stage 2, когда будет чем управлять.
- **Совместной отмены выполняющегося задания нет.** Отменяется только ожидающее:
  прерывать импорт на середине нечем.
- **Кэш диагностики межпроцессный не заводился.** При нескольких процессах API каждый
  считает состояние сам; на текущих объёмах это дешевле, чем согласовывать кэш.

## Дефекты, найденные приёмкой на стенде

Четыре дефекта поймались только запуском на живой базе, и все четыре — того класса,
который офлайн-проверка поймать не могла:

1. `asyncpg` не принимает несколько команд в одном запросе — триггер журнала не ставился;
2. `SET search_path` открывал транзакцию, и откат миграций коммитил только savepoint;
3. повторный именованный аргумент в тестовом помощнике;
4. устаревший статус задания после сырого `UPDATE` — `session.get` отдавал объект из
   identity map.

Вывод для Stage 2, записанный здесь намеренно: **код, работающий с базой, проверяется
только базой.** Офлайн-сверка DDL с моделями полезна, но подтверждает структуру, а не
поведение драйвера.
