# Аудит репозитория перед bootstrap — 2026-09-04

Промт 01 требует аудита до любых изменений. Ниже — состояние на момент начала работ.

## Что было

Каталог `Odintsov/Quantor/` содержал ровно один файл — `QTO_Portal_Stage1_ClaudeCode_Opus5.zip`
(39 КБ, prompt-pack Stage 1). Ни git-репозитория, ни исходников, ни конфигурации не было.

Проверено: package managers, Python-окружения, Docker/CI-конфигурация, миграции, тесты,
существующие приложения — ничего из перечисленного в каталоге не найдено.

Вывод: существующую архитектуру ломать не требуется, действует «пустой репозиторий»
из промта 01.

## Что учтено извне

- `Odintsov/CLAUDE.md` — общие правила для всех проектов каталога. Расхождение по стеку
  оформлено как [ADR-0000](../adr/0000-otstuplenie-ot-obshchego-steka.md).
- Соседние проекты (`FOT`, `HUBTender`, `MaterialBase`, `Meridian`) используют
  React+Vite / Express / Supabase. Общего кода с ними нет, переиспользовать нечего.

## Окружение разработчика (проверено фактически)

| Инструмент | Версия                       | Комментарий                                                                |
| ---------- | ---------------------------- | -------------------------------------------------------------------------- |
| Node.js    | 24.14.1                      | требуется ≥ 20.9                                                           |
| pnpm       | 11.10.0                      | менеджер пакетов монорепозитория                                           |
| Python     | 3.14.4 (по умолчанию) и 3.12 | используется 3.12, см. [ADR-0009](../adr/0009-python-i-lokalnyj-tuling.md) |
| Docker     | 29.5.3                       | демон на машине разработки не запущен                                      |
| git        | 2.53.0                       | репозиторий инициализирован в ходе работ                                   |

## Тестовая фикстура

`01-03-00-01-12_ПД-00260560-АР.zip` (47 МБ) получен и помещён в
`_prompts/stage1/fixtures/legacy/` (вне git). Состав подтверждён программно и совпадает
с `reference/EXPORT_ANALYSIS.md`:

`schema_version=1`, `coordinate_space=normalized_page_top_left`, 77 страниц, 383 блока
(230 text / 90 image / 63 stamp), 380 прямоугольников и 3 полигона, 320 блоков с `crop_url`.

Дополнительно проверено: имена файлов внутри архива помечены флагом UTF-8 (`flag_bits=0x0800`),
то есть кириллица читается штатно. Импортёр всё равно обязан быть терпимым к архивам без
этого флага — такие пакеты встречаются у старых упаковщиков.

## Сознательно отложено

- Доменные сущности и API проектов — промт 03.
- Дизайн-система и трёхпанельная оболочка — промт 04.
- Загрузка файлов и импорт распознанного пакета — промты 05–06.
- Просмотрщик PDF и overlay регионов — промт 07.
- Контракты Model Gateway и будущих job-типов — промт 08.
- Аутентификация и разграничение доступа: сейчас работает dev-режим без пользователей.
- Redis, очереди, воркеры: нет потребителя, добавление отложено до реальной нагрузки.
