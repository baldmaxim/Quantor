# Промт 16 — приёмка Stage 2A

Дата: 2026-09-09. Коммит первой сверки: `b704016`.

> **Обновление того же дня.** Три FAIL закрыты работой, а не переоценкой: эндпоинт величины
> и показ добавлены после приёмки (раздел 7). Гейты 7, 8 и 10 пересмотрены по факту кода.

Приёмка независимая: гейты сверялись кодом и прогонами, а не отчётами предыдущих промтов.
Одно расхождение с ними нашлось и описано в разделе 3.

## Итог

```text
PASS      7
FAIL      0
BLOCKED   8
```

**Stage 2A не закрыт.** Восемь гейтов ждут стенда, `takeoff.manual` остаётся выключенным.

## 1. Прогоны

| Команда                      | Результат                                                       |
| ---------------------------- | --------------------------------------------------------------- |
| `pnpm lint`                  | чисто                                                           |
| `pnpm typecheck`             | чисто, 109 файлов Python                                        |
| `pnpm api-client:check`      | клиент соответствует контракту                                  |
| `pnpm test`                  | pytest **459 прошло / 214 пропущено**, web **281**, admin **8** |
| `pnpm build`                 | собирается                                                      |
| `pnpm benchmark:measurement` | **16 из 16** в допуске, все воспроизводимы                      |

Миграции: одна голова `0010_takeoff_domain`, цепочка линейна, оба направления
разворачиваются офлайн (`alembic upgrade base:head --sql`, `downgrade head:base --sql`).
`alembic check` и живой исполнитель требуют базы — не выполнены.

**214 пропущенных теста — это тесты на базе.** Они не «лишние»: в них лежит почти всё
поведение продукта, включая границы арендатора и сохранность обмеров. Отсюда и шесть
BLOCKED ниже.

## 2. Пятнадцать гейтов

### 1. PREVIOUS_STAGE_GREEN — BLOCKED

Свежего полного прогона Stage 1.5 нет: 214 тестов на базе здесь пропущены. Последний
зелёный прогон с нулём пропусков был на стенде владельца, коммит `02d1782` — до промтов
02–15. Считать его свежим нельзя.

**Чем закрыть:** `pnpm infra:up && pnpm db:migrate && pnpm test` на стенде.

### 2. CANONICAL_GEOMETRY — PASS

Геометрия читается из PDF провайдером `pypdf 6.18.0`, растровая ширина в расчёте не
участвует.

**Доказательства:**

- `apps/api/tests/test_live_fixture_geometry.py` — 77 страниц настоящего чертежа, 6 проверок;
- `test_engine_never_reads_the_raster_sheet` — разбор синтаксического дерева `quantity.py`:
  `Sheet` не импортируется, `width_px` не читается, `display_width_pt` читается;
- `apps/api/tests/test_page_geometry.py`.

_Оговорка:_ запись `PageGeometry` в базу заданием исполнителя покрыта тестами, которые
здесь пропущены.

### 3. CROSS_RUNTIME_COORDINATES — PASS

`tests/fixtures/coordinate_vectors.json` сверяется **точным равенством** обеими средами:
`apps/api/tests/test_coordinate_transform.py` и `apps/web/src/lib/viewer/measurement.test.ts`.
Оба прогона зелёные и базы не требуют.

### 4. SCALE_CORRECTNESS — PASS

Горизонталь, вертикаль и диагональ сходятся. Диагональ — та проверка, ради которой этап и
начался: прежний `units_per_normalized` мог сойтись по осям и разойтись по диагонали.

**Доказательства:**

- замер, случаи `line.landscape_half_width` (10 м) и `line.landscape_half_height` (5 м) —
  одна и та же доля 0,5 по разным осям даёт разные метры, как и должно;
- `line.diagonal_3_4_5` — точное 5 м;
- `test_model_has_no_normalized_factor`, `test_contracts.py:218` — поля
  `units_per_normalized` в рабочем контракте нет.

### 5. MULTI_SCALE_SAFETY — PASS

Модель данных допускает явную калибровку у каждого измерения:
`Measurement.scale_calibration_id` — внешний ключ с `ondelete="NO ACTION"`, у листа
несколько калибровок, действующая ровно одна (частичный уникальный индекс
`uq_scale_calibrations_default_per_sheet ... WHERE is_default`).

_Оговорка:_ поведенческий тест `test_second_calibration_does_not_change_the_first` здесь
пропущен — это шаг 13 живой приёмки.

### 6. MANUAL_COUNT — BLOCKED

«Метки сохраняются после перезагрузки» проверяется только на работающем портале.

### 7. MANUAL_LENGTH — BLOCKED (было FAIL)

Путь достроен: `GET /api/v1/sheets/{sheet_id}/quantities` отдаёт величину каждого измерения
листа, инспектор её показывает.

Правильность самого расчёта доказана здесь — 16 аналитических случаев замера и тесты
движка. Сквозная проверка «линия во всю ширину листа возвращает 10 метров» написана
(`test_quantity_api.py`) и требует базы.

### 8. MANUAL_AREA — BLOCKED (было FAIL)

То же самое. Площадь считается верно (`polygon.full_sheet` — 100 м², `half_sheet_triangle` —
50 м²) и теперь доходит до клиента. Сквозная проверка на листе 10 × 10 метров ждёт базы.

### 9. EDITING — BLOCKED

Сторона просмотрщика зелёная: выбор, перетаскивание вершины и удаление проверены в
`DrawingViewport.test.tsx` и `measurement-overlay.test.ts`, включая то, что жест не
перерисовывает страницу и не порождает коммитов React.

Сохранность через API (`update_geometry`, конфликт версий, мягкое удаление) покрыта тестами
на базе — здесь пропущены.

### 10. PROVENANCE — PASS (было FAIL)

Ответ с величиной появился, и происхождение в нём **обязательное**, а не опциональное: поля
`rule_key`, `rule_version`, `page_geometry_fingerprint`, `scale_calibration_id`,
`input_fingerprint`, `verification_state` объявлены без значений по умолчанию, лист и ревизия
— на уровне ответа. Величина без цепочки объяснения не сериализуется в принципе.

Это проверяется **без базы**: `test_contract_carries_every_link_of_the_chain` разбирает поля
схемы. Проверка статическая намеренно — поле, выпавшее из ответа, обязано ломать сборку у
всех, а не только там, где поднят PostgreSQL.

К цепочке добавлено недостающее звено: `takeoff_item_id`. Величина без ответа на вопрос
«чего именно» непроверяема так же, как величина без основания.

### 11. TENANT_SECURITY — BLOCKED

Тесты написаны и их много — чужая строка обмера, чужая калибровка, чужое измерение, чужой
лист через пакет, угаданные идентификаторы, системные задания. Все требуют базы и здесь
пропущены.

Статически проверено только одно: `test_routes_deny_by_default` — маршрут без явного права
недоступен.

### 12. PERFORMANCE — BLOCKED

Замер 1k/5k/10k снят и записан (`benchmark-report.md`): рост времени кадра линейный, попадание
на десяти тысячах фигур перестаёт укладываться в кадр. Это половина гейта.

Вторая половина — «живой проект остаётся отзывчивым» — требует стенда. Задержки API и число
запросов помечены `skipped` в самом замере.

### 13. NO_AI — PASS

**Доказательства** (все без базы):

- `test_no_model_or_vision_sdk_in_dependencies` — ни в одном пакете нет SDK моделей;
- `test_no_job_type_invokes_a_model` — ни один тип задания не соответствует шагу с моделью;
- `test_quantity_calculation_never_uses_models`;
- `test_model_provider_has_no_implementations` — шлюз объявлен, реализаций нет;
- `test_ordinary_admin_page_load_makes_no_model_call`.

Флаги `takeoff.ai` и `models.gateway` выключены и не редактируются администратором.

### 14. LIVE_PDF — BLOCKED

Подробно — [15-live-acceptance.md](15-live-acceptance.md) и [live-acceptance.md](live-acceptance.md).

Эталон на месте и прочитан: 77 страниц за 0,35 с, 47 повёрнутых листов, стороны переставлены
на всех. Остальные 13 шагов требуют стенда и человека у экрана.

### 15. FEATURE_GATE — PASS

Правило соблюдено: гейты не зелёные, `takeoff.manual` выключен. Проверено кодом —
`_stage2(...)` задаёт `default=False, admin_editable=False`.

Включение флага не создаёт функциональность, а показывает её тем, у кого она может быть
сломана. При трёх FAIL это было бы прямым вредом.

## 3. Расхождение с отчётами промтов

Отчёт промта 12 записал в остаток: «эндпоинта величины пока нет… Подключение к API и UI —
вместе с замером в промтах 13–14».

**Этого не произошло.** Промт 13 занимался замером, промт 14 — границами и устойчивостью;
подключения не требовал ни один, и работа выпала между ними. Ни один последующий отчёт
этого не заметил, потому что каждый проверял своё.

Так и появились три FAIL. Ошибка не в расчёте — расчёт верен и проверен на шестнадцати
случаях; ошибка в том, что результат некому показать.

## 4. Архитектурная сверка

| Утверждение                                        | Чем доказано                                                               | Итог            |
| -------------------------------------------------- | -------------------------------------------------------------------------- | --------------- |
| `units_per_normalized` убран из рабочего контракта | `test_contracts.py:218`, `test_scale_calibration.py:592`                   | да              |
| Растровая ширина не участвует в физическом расчёте | разбор AST `quantity.py`                                                   | да              |
| Поворот не применяется дважды                      | 47 повёрнутых листов эталона; `placeRenderedPage` возвращает `rotation: 0` | да              |
| Модель нескольких масштабов безопасна              | явный ключ у измерения, частичный уникальный индекс                        | да              |
| `Measurement` отделён от `Region`                  | разные таблицы, модели и слои отрисовки (ADR-0008, ADR-0019)               | да              |
| Величины детерминированы и версионированы          | `count.v1`, `length.v1`, `area.v1`; 16 случаев воспроизводимы              | да              |
| Вызовов моделей нет                                | пять тестов, см. гейт 13                                                   | да              |
| Нет молчаливого сложения по ревизиям               | `test_total_requires_an_explicit_scope` — область обязательна              | да              |
| Нет утечек между арендаторами                      | тесты есть, здесь пропущены                                                | не подтверждено |
| Решение Canvas/WebGL основано на замере            | [13-benchmark.md](13-benchmark.md), [14-hardening.md](14-hardening.md)     | да              |

## 5. Точный остаток

Работы, которую можно сделать без стенда, не осталось. Всё оставшееся — прогон.

1. Полный `pnpm test` без пропусков — гейты 1, 6, 7, 8, 9, 11.
2. Живая приёмка по бланку [live-acceptance.md](live-acceptance.md) — гейт 14.
3. Раздел живой базы в замере — вторая половина гейта 12.
4. `alembic check` и прогон исполнителя.

Только после этого имеет смысл разговор о `takeoff.manual`.

## 7. Обновление: величина доведена до экрана

Сделано после первой сверки, поэтому вынесено отдельно — чтобы не выглядело, будто гейты
были зелёными с самого начала.

| Что                    | Где                                                                                |
| ---------------------- | ---------------------------------------------------------------------------------- |
| Эндпоинт величин листа | `GET /api/v1/sheets/{sheet_id}/quantities`                                         |
| Сборка данных          | `takeoff_service.quantities_for_sheet` — три запроса независимо от числа измерений |
| Схемы ответа           | `MeasurementQuantityRead`, `TakeoffItemQuantityRead`, `SheetQuantitiesRead`        |
| Показ величины         | инспектор измерения                                                                |
| Итог по строке         | панель обмеров                                                                     |
| Клиент                 | перегенерирован                                                                    |

Решения по дороге:

- **Один запрос на лист, а не на измерение.** Сотня обращений ради сотни меток была бы тем
  же N+1, только со стороны клиента. Проверено счётчиком запросов: их число не меняется от
  числа измерений.
- **`quantity.py` остался чистым.** Загрузка живёт в сервисе обмера, расчёт по-прежнему не
  знает ни про сессию, ни про порядок запросов — на этом держится воспроизводимость, и за
  этим следит тест на разбор синтаксического дерева.
- **Измерение считается по своей калибровке, а не по действующей.** Подстановка действующей
  означала бы, что величина, показанная заказчику вчера, сегодня другая, и никто этого не
  заметил. Проверено отдельным тестом с двумя калибровками.
- **`QuantityState` переехал в домен**: он виден в контракте, а контракту не место
  зависеть от модуля сервиса.

### Попутно исправлена ошибка в панели обмеров

Строка показывала число измерений с единицей строки: «3 м» означало три линии, а читалось
как три метра. Теперь показывается величина, а число измерений ушло в подпись. Недоступные
считаются отдельно — приписка «+N без масштаба», а не молчаливое включение нулями.

### Проверки после доработки

| Проверка                                    | Результат                      |
| ------------------------------------------- | ------------------------------ |
| `pytest`                                    | **461 прошло / 223 пропущено** |
| `vitest` web                                | **286**                        |
| `ruff`, `mypy`, `eslint`, `tsc`, `prettier` | чисто                          |
| `pnpm build`                                | собирается                     |

Новых миграций нет: величина считается по запросу.

### Замеченное и не объяснённое

В одном прогоне из четырёх `pytest` показал 4 падения, которые не воспроизвелись ни в одном
последующем; имена не сохранились — вывод был отфильтрован. Выдавать это за случайность
нельзя, поэтому записано как есть.

Заодно исправлена хрупкость, внесённая в промте 15: тест эталонного чертежа писал временный
файл под постоянным именем в общий каталог. Два одновременных прогона затирали бы файл друг
друга, и падение выглядело бы как «иногда не читается PDF». Теперь каталог выдаёт pytest.

При вашем прогоне на стенде стоит обратить внимание, не появятся ли эти падения снова.

## 6. Передача

[HANDOFF_TO_STAGE2B.md](HANDOFF_TO_STAGE2B.md).

Stage 2B не начат и начат не будет до отдельного пакета владельца.

**STOP.**
